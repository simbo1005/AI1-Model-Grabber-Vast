name: Build and publish Vast.ai Docker image

on:
  workflow_dispatch:
    inputs:
      tag:
        description: Docker image tag
        required: true
        default: "1.0"

permissions:
  contents: read

env:
  IMAGE_NAME: sdcioba/comfyui-workflow-launcher-vast

jobs:
  publish:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Test launcher
        run: |
          python -m pip install -r requirements-launcher.txt pytest
          python -m pytest

      - name: Audit custom-node requirements
        run: python scripts/audit_node_requirements.py

      - name: Ensure public Docker Hub repository exists
        env:
          DOCKERHUB_TOKEN: ${{ secrets.DOCKERHUB_TOKEN }}
        run: python scripts/ensure_dockerhub_repository.py

      - name: Free runner disk space
        run: |
          echo "Disk space before cleanup:"
          df -h /
          sudo rm -rf /usr/local/lib/android
          sudo rm -rf /usr/share/dotnet
          sudo rm -rf /opt/ghc
          sudo rm -rf /usr/local/.ghcup
          sudo rm -rf /usr/local/share/boost
          sudo apt-get clean
          docker system prune --all --force
          echo "Disk space after cleanup:"
          df -h /

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Sign in to Docker Hub
        uses: docker/login-action@v3
        with:
          username: sdcioba
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Build and push
        uses: docker/build-push-action@v6
        with:
          context: .
          platforms: linux/amd64
          push: true
          tags: |
            ${{ env.IMAGE_NAME }}:${{ inputs.tag }}
            ${{ env.IMAGE_NAME }}:latest
          build-args: |
            IMAGE_VERSION=${{ inputs.tag }}
          cache-from: type=gha
          cache-to: type=gha,mode=min
